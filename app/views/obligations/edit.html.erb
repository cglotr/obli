<%= render partial: 'layouts/content_header', locals: { title: @title, links: @links } %>
<%= render 'form' %>
<%= link_to 'Delete', obligation_path(@obligation),
  method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-link btn-block"
%>
