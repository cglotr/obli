<div id="flash" class="alert border-0 d-flex flex-column justify-content-center m-0 px-3 py-0">
  <% alerts.each do |msg| %>
    <%= content_tag :div, msg.downcase.tr('.', ''), class: "text-center" %>
  <% end %>
</div>
